framework:
    messenger:
        transports:
            async: '%env(MESSENGER_TRANSPORT_DSN)%'
            sync: 'sync://'

        routing:
            App\UseCase\Catalog\SyncSet\Input: async

when@test:
    framework:
        messenger:
            transports:
                async: 'in-memory://'
